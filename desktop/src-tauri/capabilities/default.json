{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities — minimal exposure. The shell is a thin window around code.forgecart.com; we don't expose system APIs to the page beyond what's strictly needed for the wrapper itself.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:webview:default",
    "core:window:default",
    "core:event:default",
    "shell:allow-open",
    "window-state:default"
  ]
}
